import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'cap-link-crash-demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
